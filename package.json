{
  "name": "terminal",
  "version": "1.0.0",
  "description": "Test app to interpret basic commands as input on the REPL",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node out",
    "build": "tsc"
  },
  "keywords": [
    "terminal",
    "interpreter",
    "test",
    "shell"
  ],
  "author": "Sourav Dey",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^4.1.0",
    "events": "^3.3.0",
    "readline-sync": "^1.4.10"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/readline-sync": "^1.4.8",
    "typescript": "^5.7.3"
  }
}
